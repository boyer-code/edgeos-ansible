---
#ansible-playbook ~/edgeos-ansible/edgeos_vlan.yaml -k -i ~/edgeos-ansible/joey-inventory.yaml
hosts: erx
  #connection: network_cli #define connection in inventory as "ansible_connection: network_cli"
#ansible_network_os: edgeos you might think specifying these in the playbook is a good idea, but it's not...put them in inventory variables
#ansible_user: ubnt         you might think specifying these in the playbook is a good idea, but it's not...put them in inventory variables

tasks:
  - name: Get config for EdgeOS devices
    edgeos_facts:
      gather_subset: all
  - name: Display the config
    debug:
      msg: "The hostname is {{ ansible_net_hostname }} and the OS is {{ ansible_net_version }}"

  - name: Add VLANs to EdgeOS devices
    edgeos_facts:
      gather_subset: all
  - name: Display the config
    debug:
      msg: "The hostname is {{ ansible_net_hostname }} and the OS is {{ ansible_net_version }}"